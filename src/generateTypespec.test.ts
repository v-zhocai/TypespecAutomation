import { test } from "./common/utils"

// test("generate demo", async ({ launch }) => {
// const { page } = await launch({
//   workspacePath: "./CreateTypespecProject"
// })
// await installExtension(page)
// })
